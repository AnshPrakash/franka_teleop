[metadata]
name = franka_teleop
version = 0.0.0
description = Franka teleoperation + recorder (ROS 2 Humble)

[options]
packages = find:
install_requires =
    numpy
    hydra-core
    omegaconf
zip_safe = True

[options.packages.find]
where = .
exclude =
    tests

    